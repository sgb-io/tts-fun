# URL of the FastAPI backend (used by Next.js server-side rewrites).
# In Docker this is the internal service name; in local dev it's localhost.
API_URL=http://localhost:8000

# Set to "dev" to run `npm run dev` (hot-reload) instead of `npm start`.
NEXT_MODE=production
