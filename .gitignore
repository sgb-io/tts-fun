# Model weights — large binary files, never commit
checkpoints/openaudio-s1-mini/

# User reference voices (optional — remove if you want to version-control them)
references/*/

# Podcast episode data (audio clips, final audio, episode JSON)
podcast_data/*/

# Environment
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
web/.venv/

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

hf_access_token.txt

# we only have local node_modules for IDE features
frontend/node_modules
# we only have a local copy for IDE features
frontend/package-lock.json